---
import { Image } from "astro:assets";
import { FOOTER } from "../config";
import { getLanguageFromURL } from "../languages";

export type Props = {
  currentPage: string;
};

const { currentPage } = Astro.props;
const langCode = getLanguageFromURL(currentPage);
const footers = FOOTER[langCode];

---

{
  Object.entries(footers).map(([_L, footer]) => (
    <footer class="flex flex-1 flex-col items-center gap-[90px] bg-u+pinky px-5 py-10 lg:flex-row md:px-[5.625rem]">
      <Image src={footer.logo} alt="uraohjain" width={169} height={182} />
      <div class="flex max-w-[65vw] flex-1 flex-col">
        <h3 class="pb-4 text-[2rem] font-bold text-u+burg md:text-[2.5rem]">
          {footer.title}
        </h3>
        <span class="text-[1rem] font-light ">{footer.description}</span>
      </div>
      <Image
        src={footer.eu}
        alt="europaan union tuki"
        width={120}
        height={119}
      />
    </footer>
  ))
}
